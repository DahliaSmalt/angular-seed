describe('AngularSeed Feature Spec', function() {
  it('should do something', function() {
    browser.get('http://www.angularjs.org');

    var foo = 'Bar';

    expect(foo).toEqual('Bar');
  });
});